import{h,card,badge}from'./ui.js';export const title='Synchronization';export const subtitle='Desired/observed revision reconciliation across Mission Control, ROS 2, Sionna, and Isaac Sim.';export async function render(root,s,{post}){const x=s.sync||{},st=x.status||{};root.append(h('div',{class:'grid'},card('Revision State',h('div',{class:'form-grid'},...['desired','current','status','isaac_ack'].map(k=>h('div',{class:'kpi'},h('strong',{},x[k]?.revision_id||x[k]?.stage||'—'),h('span',{},k)))),'span-8'),card('Reconciliation',h('div',{},h('div',{class:'toolbar'},badge(st.stage||'UNKNOWN',st.stage==='COMMITTED'?'good':'warn'),h('button',{class:'primary',onclick:()=>post('/api/command',{action:'smoke_test'})},'Verify'),h('button',{onclick:()=>window.netlab.post('/api/command',{action:'repair'})},'Repair Runtime')),h('p',{},'A change remains pending until required participants acknowledge the same revision and component hashes.')),'span-4'),card('Advanced Inspector',h('pre',{},JSON.stringify(x,null,2)),'span-12')))}
