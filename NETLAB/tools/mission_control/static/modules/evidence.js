import{h,card}from'./ui.js';export const title='Evidence';export const subtitle='Run manifests, configurations, metrics, events, revision history, screenshots, video, bags, and provenance.';export async function render(root,s,{get}){const e=await get('/api/evidence');root.append(card('Evidence Index',h('div',{class:'table-wrap'},h('table',{},h('thead',{},h('tr',{},h('th',{},'File'),h('th',{},'Bytes'),h('th',{},'Modified'))),h('tbody',{},...e.files.map(x=>h('tr',{},h('td',{},x.name),h('td',{},x.size),h('td',{},new Date(x.mtime*1000).toISOString())))))))}
