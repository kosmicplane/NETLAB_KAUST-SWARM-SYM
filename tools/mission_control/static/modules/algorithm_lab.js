import{h,card}from'./ui.js';export const title='Algorithm Lab';export const subtitle='Isolated researcher plugins for control, trajectory, formation, topology, routing, recovery, RF, traffic, energy, and metrics.';export async function render(root,s,{get}){const p=await get('/api/plugins');root.append(h('div',{class:'grid'},card('Installed Plugin SDK Examples',h('div',{class:'table-wrap'},h('table',{},h('thead',{},h('tr',{},h('th',{},'Plugin'),h('th',{},'Manifest'))),h('tbody',{},...p.plugins.map(x=>h('tr',{},h('td',{},x.name),h('td',{},x.path))))),'span-8'),card('Safety Boundary',h('p',{},'Plugin output is validated for entity IDs, finite values, coordinate frame, units, timestamps, geofence, dynamics, minimum separation, and schema compatibility. Isolated workers use timeouts and safe fallback behavior.'),'span-4')))}
