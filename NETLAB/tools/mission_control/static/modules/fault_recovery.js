import{h,card,jsonEditor}from'./ui.js';export const title='Fault & Recovery';export const subtitle='Cyber-physical failure schedules, outage evidence, candidate routes, standby promotion, and feasibility-gated service restoration.';export async function render(root,s,{post}){let f=structuredClone(s.config?.failures||{});const out=h('pre',{},'No failure revision.');root.append(h('div',{class:'grid'},card('Failure Schedule & Recovery Policy',h('div',{},jsonEditor(f,v=>f=v),h('button',{class:'danger',onclick:async()=>out.textContent=JSON.stringify(await post('/api/failures/apply',{failures:f}),null,2)},'Apply Failure Revision')),'span-8'),card('Recovery Acceptance',h('p',{},'Recovery completes only after route computation, current Sionna metrics, ROS application acknowledgement, Isaac scene acknowledgement, a feasible execution gate, and observed packet resumption.'),'span-4'),card('Result',out,'span-12')))}
