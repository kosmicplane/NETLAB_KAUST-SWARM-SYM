# Configuration reference

The authoritative JSON configuration contains experiment, clock, world, service region, station, swarm, topology, communication, antennas, traffic, failures, visualization, evidence, and runtime sections. SI units are used internally. Coordinates declare ENU, NED, ECEF, WGS84, or local stage frame. Configuration validation enumerates every failing JSON path and error code.
