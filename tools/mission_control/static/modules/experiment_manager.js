import{h,card}from'./ui.js';export const title='Experiment Manager';export const subtitle='Templates, replications, parameter sweeps, deterministic seeds, comparison, replay, and evidence.';export async function render(root,s,{get}){const sc=await get('/api/scenarios');root.append(h('div',{class:'grid'},card('Validated Scenario Library',h('div',{class:'table-wrap'},h('table',{},h('thead',{},h('tr',{},h('th',{},'Scenario'),h('th',{},'Path'))),h('tbody',{},...sc.scenarios.map(x=>h('tr',{},h('td',{},x.name),h('td',{},x.path))))),'span-8'),card('Run Design',h('div',{},h('p',{},'Select grid, random, Latin-hypercube, imported, or plugin-defined designs. Every run preserves seed, hashes, image digests, model versions, and evidence provenance.'),h('button',{class:'primary'},'Create Run Group')),'span-4')))}
